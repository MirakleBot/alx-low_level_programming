#include "main.h"
/**
 * _isupper - fuction checks for uppercase character
 * @c: the character to check
 * Return: 1 if c is uppercase, otherwise return 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
