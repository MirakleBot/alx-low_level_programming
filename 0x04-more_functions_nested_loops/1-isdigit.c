#include "main.h"
/**
 * _isdigit - function checks for a digit
 * @c: digit to check for
 * Return: 1 if c is digit, 0 otherwise
 */

int _isdigit(int c)
{
	int c;

	c = 0;
		for (c <= 9)
		{
			_putchar('c');
		}
		return (1);
}
